---
export interface Props {
  title: string;
  description: string;
  category: string;
  filterKey: string;
  image: string;
  slug: string;
  index: number;
}

const { title, description, category, filterKey, image, slug, index } = Astro.props;
---

<article
  class="project-card"
  data-project-card
  data-filter-key={filterKey}
  style={`--stagger: ${index * 90}ms`}
>
  <a href={slug} class="project-card__link">
    <div class="project-card__media">
      <img
        src={image}
        alt=""
        width="1200"
        height="800"
        class="project-card__image"
        loading={index === 0 ? "eager" : "lazy"}
        fetchpriority={index === 0 ? "high" : "auto"}
        decoding="async"
      />
    </div>

    <div class="project-card__content">
      <div class="project-card__meta">
        <p class="project-card__category">{category}</p>
        <span class="project-card__number" aria-hidden="true">
          {String(index + 1).padStart(2, "0")}
        </span>
      </div>
      <h2 class="project-card__title">{title}</h2>
      <p class="project-card__description">{description}</p>
      <span class="project-card__action">
        View project
        <svg viewBox="0 0 16 16" aria-hidden="true">
          <path d="M3 8h10M9 4l4 4-4 4"></path>
        </svg>
      </span>
    </div>
  </a>
</article>

<style>
  .project-card {
    --discipline-accent: var(--color-text-light);
    min-width: 0;
    overflow: hidden;
    border: 1px solid var(--color-border);
    border-radius: 0.75rem;
    background: var(--color-body);
    transition:
      opacity 0.55s ease var(--stagger),
      transform 0.55s ease var(--stagger),
      border-color 0.25s ease,
      box-shadow 0.25s ease;
  }

  .project-card[data-filter-key="ux-ui"] {
    --discipline-accent: #1f6faf;
  }

  .project-card[data-filter-key="web"] {
    --discipline-accent: #a84900;
  }

  .project-card[data-filter-key="product"] {
    --discipline-accent: #6b4bb5;
  }

  .project-card.is-reveal-pending {
    opacity: 0;
    transform: translateY(1.25rem);
  }

  .project-card.is-visible {
    opacity: 1;
    transform: none;
  }

  .project-card__link {
    display: flex;
    height: 100%;
    flex-direction: column;
  }

  .project-card__media {
    position: relative;
    aspect-ratio: 4 / 3;
    overflow: hidden;
    background: var(--color-light);
  }

  .project-card__image {
    width: 100%;
    height: 100%;
    padding: 0.75rem;
    object-fit: contain;
    transition: transform 0.55s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .project-card__number {
    display: grid;
    flex: none;
    width: 2.35rem;
    height: 2.35rem;
    place-items: center;
    border: 1px solid var(--color-border);
    border-radius: 999px;
    background: rgb(255 255 255 / 0.92);
    color: var(--color-text-dark);
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.08em;
  }

  .project-card__action {
    position: relative;
    display: inline-flex;
    align-self: flex-end;
    align-items: center;
    gap: 0.45rem;
    margin-top: auto;
    padding-top: 1.25rem;
    color: var(--color-text-dark);
    font-size: 0.78rem;
    font-weight: 700;
    isolation: isolate;
  }

  .project-card__action::after {
    position: absolute;
    z-index: -1;
    right: 0;
    bottom: -0.2rem;
    left: 0;
    height: 0.2rem;
    background: #eff93b;
    content: "";
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 0.22s ease;
  }

  .project-card__action svg {
    width: 1rem;
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.5;
    transition: transform 0.2s ease;
  }

  .project-card__content {
    display: flex;
    flex: 1;
    flex-direction: column;
    padding: 1.25rem;
  }

  .project-card__meta {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 0.6rem;
  }

  .project-card__category {
    margin: 0;
    color: var(--discipline-accent);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.13em;
    text-transform: uppercase;
  }

  .project-card__title {
    margin: 0 0 0.65rem;
    font-size: clamp(1.25rem, 2vw, 1.5rem);
    font-weight: 700;
    letter-spacing: -0.015em;
  }

  .project-card__description {
    margin: 0;
    color: var(--color-text-light);
    font-size: 0.9rem;
    line-height: 1.6;
  }

  .project-card:focus-within {
    border-color: var(--color-text-dark);
    box-shadow: 0 1rem 2.5rem rgb(0 0 0 / 0.1);
  }

  .project-card__link:focus-visible {
    outline: 3px solid #eff93b;
    outline-offset: -3px;
  }

  @media (hover: hover) and (pointer: fine) {
    .project-card:hover {
      border-color: #d4d4d4;
      box-shadow: 0 1rem 2.5rem rgb(0 0 0 / 0.1);
      transform: translateY(-0.35rem);
    }

    .project-card:hover .project-card__image {
      transform: scale(1.025);
    }

    .project-card:hover .project-card__action svg {
      transform: translateX(0.2rem);
    }

    .project-card:hover .project-card__action::after {
      transform: scaleX(1);
      transform-origin: left;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .project-card,
    .project-card__image,
    .project-card__action::after,
    .project-card__action svg {
      transition: none;
    }

    .project-card {
      opacity: 1;
      transform: none;
    }
  }
</style>
